<Window x:Class="SCPIMCMain.View.Controls.MacroEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:SCPIMCMain.ViewModel.Controls"
        xmlns:enums="clr-namespace:SCPIMCMain.Common.Enum"
        mc:Ignorable="d"
        Title="매크로 설정"
        Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=viewModel:MacroEditorViewModel}">
    <Window.Resources>
        <ObjectDataProvider x:Key="CommandTypeEnum" MethodName="GetValues" ObjectType="{x:Type enums:ECommandType}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:ECommandType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Macro Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="매크로 이름" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding MacroName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Macro Description -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="설명" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding MacroDescription, UpdateSourceTrigger=PropertyChanged}"
                     Height="60"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Hotkey Key Code -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="단축키 코드 (선택사항)" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding MacroHotkeyKeyCode, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Command Chain Header -->
        <DockPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="명령어 체인" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="추가"
                        Command="{Binding AddCommandCommand}"
                        Width="70"
                        Height="25"
                        Margin="0,0,5,0"/>
                <Button Content="삭제"
                        Command="{Binding RemoveCommandCommand}"
                        Width="70"
                        Height="25"/>
            </StackPanel>
        </DockPanel>

        <!-- Command Chain List -->
        <DataGrid Grid.Row="4"
                  ItemsSource="{Binding Commands}"
                  SelectedItem="{Binding SelectedCommand}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="타입"
                                        Width="100"
                                        SelectedItemBinding="{Binding CommandType}">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Source={StaticResource CommandTypeEnum}}"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Source={StaticResource CommandTypeEnum}}"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="명령어 / 지연시간(ms)"
                                    Width="*"
                                    Binding="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="저장"
                    Click="SaveButton_Click"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"/>
            <Button Content="취소"
                    Click="CancelButton_Click"
                    Width="80"
                    Height="30"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
